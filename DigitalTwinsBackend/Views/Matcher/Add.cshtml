@model IEnumerable<DigitalTwinsBackend.Models.Matcher>

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Select available Matchers to add to the UDF</h2>

<h4>Add</h4>
<hr />

<div class="row">
    <div class="col-md-12">
        <form asp-action="Add" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            Add to udf
                        </th>
                        <th>
                            Name
                        </th>
                        <th>
                            Condition
                        </th>
                        <th>
                            Origin Space Id
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @Html.EditorForModel()
                </tbody>
            </table>

            <div class="form-group">
                <input type="submit" name="updateButton" value="Add" class="ButtonNew" />
                <input type="submit" name="updateButton" value="Cancel" class="ButtonCancel" />
            </div>
        </form>
    </div>
</div>

